/*
Write a make file which generates a shared object from the Shape Assignment. Then write another 
application which uses the share object and creates shapes
*/

#include "include.h"
#include "classes.h"

int main() {

    Rectangle rect;

    rect.setSize(side,otherside);

    std::cout << "Area: " << rect.area() << std::endl;


    return 0;
}