cmake_minimum_required(VERSION 3.5.1)
project(bonus2)
include_directories(include)
set(SOURCE_DIR src/)
add_library(pcapOpenner SHARED ${SOURCE_DIR}pcapOpenner.cpp)
add_executable(main ${SOURCE_DIR}main.cpp)
target_link_libraries(main pcapOpenner pcap)
add_dependencies(main pcapOpenner)